import React from 'react';
import { Text, View, StyleSheet } from 'react-native';
import Constants from 'expo-constants';
import MapContainer from '../search/MapContainer';


export default CourseDetails = (props) => {
    return (
        <MapContainer />
    );
}